#include <stream>
int main() {
  if new.system(user) = created: {
    new.variable('number.name.generated'): {
      while iostream = system; {
        random.number.variable() }
    new.name('X-{number.name.generated}')
      }
  }
